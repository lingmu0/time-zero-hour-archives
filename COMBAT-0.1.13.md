# 0.1.13 战斗调整

本版沿用 0.1.12 的所有阶段、随机四相护盾、最终破盾召唤假身、三种场地环带与 BGM。

- 下砸：高举法杖后顺势砸向正前方中央，第 18 tick 结算，36 tick 收招。移除外撇避让和手腕侧移，以形状保持三次曲线烘焙平滑动作。允许法杖短暂穿过身体，优先保证手臂自然。书仍水平悬浮于掌上。
- 多圆环带：原 6 个增至 12 个（包含玩家脚下固定的 1 个），半径 2.5 格，圆心间隔大于 6 格，仍保留安全间隙。脚下位置在预警开始时固定，不持续追踪；实际区域与预警共用几何。本体、假身共用。
- 飞弹：0.65 → 1.05 格/tick，即标准 20 TPS 下 13 → 21 格/秒。仍从法杖顶端发射，并沿整段位移进行碰撞检测。27 方块的独立立体钟械模型：金铜钟环、象牙指针、青白矛芯；随飞行方向转向、钟环旋转、核心脉动。不使用紫水晶或平面飞弹贴图。早期生成的 2D 草稿未接入资源。
- 终末挑战徽记：`/give @s time:challenge_sigil`，也在模组创造栏内，无配方、不消耗。右键进入 `time:challenge` 独立维度内的固定挑战房并启动或加入现有 Boss 战；不需要研究进度、三枚印记、零刻钥匙，且不补发前置研究进度。仍保留完整随机护盾、阶段限伤、假身等机制。胜利保留 Boss 原本的战斗奖励。
- 挑战退出：在场内潜行右键徽记返回进入前位置附近的安全落脚点。返回记录存储于玩家持久数据；不会因再次右键加入当前战斗而覆盖。找不到安全点时取消传送并保留记录。不改变游戏模式或世界难度；和平难度拒绝启动。多人共享同一场战斗，不会通过重复右键重置活着的 Boss。弃战后沿用原本重试冷却。
- 场地保护：只在专用虚空维度的空白 41×41×14 区域初始化，不改动自然遗迹或主世界建筑。已存在挑战控制器则复用；场地异常时拒绝覆盖已有方块。床与重生锚不设为本维度重生点。

## 验证范围

未启动 Minecraft，未进行游戏内实测。按模组技能与用户“不启动测试”的要求，使用编译、纯 JVM 状态检查、资源及实际模型几何检查；不把 Blockbench 截图称为游戏截图。

- `gradlew assemble stateTest compileValidationJava --offline`：构建、纯 JVM 测试、运行时验证代码编译；不执行 GameTest/客户端。
- `verify-keeper-casts.mjs`：677 个 BB 保存关键帧与共享曲线一致。
- `verify-keeper-clearance.mjs`：816 个姿势；仅下砸法杖允许交叠，书悬浮与其他施法姿态约束仍校验。
- `verify-keeper-socket.mjs`：98 个朝向/年龄/释放点比较，真实模型与服务端插槽一致。
- `verify-combat-0.1.13.mjs`：41 个年龄下的居中落点、举杖高度、曲线连续性、飞弹立体几何与挑战入口安全接线的静态检查。
- `verify-resources.mjs`：三套模型、注册物品、图集与资源路径。
- `verify-boss-music.mjs`：BGM/唱片资源和生命周期检查；音乐本版未修改。

新增维度首次加载、实际跨维度传送/返回、多人加入与战斗视觉仍需之后由玩家进游戏验收。本版没有生成或沿用旧版本的游戏实测“通过”报告。
