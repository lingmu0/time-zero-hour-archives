# 0.1.4 战斗调整

## 规则

- 时序环带实时跟随当前施法者的水平位置，危险半径严格大于 4 格、小于 10 格，边界安全。预警 30 tick，落地冲击消散 10 tick。真身与假身共用渲染规则，象限方位仍以固定场地中心计算。
- 最终阶段无护盾、不需要中央锚解锁。开场四锚护盾保留；旧存档最终阶段的护盾在加载时清除。巨额伤害仍只能打到下一阶段血量边界。
- 假身沿场地移动并转向玩家，使用与真身相同伤害的时序飞弹、环带及两种施法动画。保留蓝色魂火、反向指针、受击反噬；真身保留白色星光。
- 终局每经过 200 个有效战斗 tick（10 秒）尝试换位。如果仍在环带预警/冲击期间或飞弹尚未出手，会稍后触发。双方同时进入 24 tick 遮蔽，暂停攻击与移动，第 12 tick 做一次 50% 随机判定。
- 真假换位均显示相同的紫色遮蔽柱、环形扫描线、闪光、传送粒子与声音，不发送结果提示。实际换位同时交换移动轨道角度，不立刻回到原轨迹；目标位置必须在场内且无方块碰撞。玩家仍可重新观察魂火/反向指针辨认。
- 现在阶段仅提示西北/东北/西南/东南，不再附带颜色名。不添加常驻安全/危险区涂层；实际施加时间停滞时显示三道电光束缚环与低音钟鸣。
- 未来债务实际造成伤害时显示闪光、紫色碎粒、暴击碎屑和音效；原债务伤害与时间紊乱规则不变。

## 验证入口

```powershell
.\gradlew.bat build runValidationServer runCombatClient --offline
node tools/verify-resources.mjs
node tools/verify-runtime-reports.mjs --since=<本轮开始的UTC时间>
node tools/verify-combat-report.mjs --since=<本轮开始的UTC时间>
```

36 项纯状态断言；10 组隔离服务端 GameTest。新增测试覆盖假身实际移动、真实投射物、环带预警及施法状态、概率换位的两条分支、相同客户端特效标记、旧终局护盾存档迁移和特效消散。

隔离 GPU 客户端包含 14 张截图：原 8 个战斗场景，加停滞/未来命中特效、未来债务真实扣血、假身飞弹前摇、假身环带、双体换位遮蔽。两张命中特效截图直接调用生产特效函数作确定性夹具；另一个未来阶段场景通过真实玩家和伤害事件确认债务扣血后及时截图（不把飞弹或环带误认为债务）。实际现在停滞由服务端 GameTest 覆盖。

报告：`build/reports/runtime-gametests.json` 与 `build/reports/combat-visual.json`；截图：`run-validation-combat/screenshots/`。以报告时间和通过状态为准，历史旧版自然遗迹截图不作为本版新测试结果。

仅修改本独立模组及隔离验证目录，未安装到整合包、未操作玩家存档或 Blockbench 源模型。客户端与服务端应一起更新到 `time-0.1.4-1.21.1.jar`。
