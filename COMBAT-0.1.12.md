# 0.1.12 战斗更新

本页说明当前规则；README 中较早版本的“终局无盾 / 以 Boss 为中心环带”为历史记录。

- 飞弹在法杖顶部钟核发射，服务端发射点由源模型骨骼链及第 10 tick 动画姿态生成，跟随身体朝向；抄写员的发射方式不变。
- 距 Boss 水平 2.8 格内、垂直相差不超过 2.5 格且无遮挡，会触发高举下砸：先把法杖举过头顶、短暂蓄力，再快速向下砸落，第 18 tick 判定命中、36 tick 完整动作、50 tick 最短冷却。命中造成基础攻击力 1.2 倍伤害和击退，杖头地面落点有扬尘、暴击粒子及重击音效。释放时重新检查距离、视线和前方角度，离开可以躲避。环带蓄力期间不插入杖击，持续贴身也不会永久拖延环带。
- 环带的预警固定在场地，1.5 秒后释放，结算后残留 0.5 秒视觉。每次随机选择与上次不同的变体：
  1. 场地中心半径 4–10 格，以及半径超过 13 格均危险；小于 4 格、10–13 格为安全间隔。
  2. 横向或纵向 3 格宽危险条带，间隔 3 格宽安全通道，起始偏移随机。
  3. 最多 6 个半径 2.5 格的圆；第一个固定在目标玩家预警出现时的位置，其余随机分布，圆心间距至少 6 格。
- 危险区用填充、边界及蓄力变亮显示，释放时出现附魔台字符粒子爆发。图形及伤害均裁剪在场地边界内。假身也使用上述环带和杖顶飞弹。
- 开场及每次阶段转换，四个相位的顺序独立随机；仅同步已点亮相位，不向玩家发送完整答案。点错清空进度，但本阶段次序不变。答案及进度保存到服务端实体 NBT；重载后恢复，旧存档没有有效次序则重新生成。
- 破盾前保留通用飞弹、环带和近身压力；过去/现在/未来阶段专属机制从破盾后开始计时。终局破盾前不生成假身；重载也遵守此限制。阶段限伤仍生效。

## 验证与源文件

沿用原生 ModelPart，无新增渲染依赖。新杖击位于 model_source/keeper-casts.json，执行 tools/export-keeper-casts.mjs 生成运行时代码。tools/export-keeper-socket.mjs 根据骨骼链生成服务端发射点；两者支持 --check。

本次最初 MCP 端口未响应，先完成本地源编辑；用户启动 Blockbench 后已连接 MCP 1.6.1，最终共 198 个关键帧；按后续反馈将短幅推杖重做为高举下砸，已检查举杖与落点姿势并保存。MCP 预览为 build/model-preview/mcp-staff-0_65.png 和 mcp-staff-0_9.png，不是游戏截图。新源项目 model_source/chronicle_keeper_casts.bbmodel 包含三套动作，旧项目备份为 chronicle_keeper_casts-before-0.1.12.bbmodel。

后续需要 MCP 而端口未响应时，用户已授权自行检查并启动 Blockbench 后重连；不能因此覆盖已打开的其他模型或未保存修改。

只执行离线编译、纯 Java 状态/几何断言、模型关键帧与资源检查，不启动 Minecraft。游戏端校验夹具同步了随机护盾规则，但本次未运行。游戏内视觉、多人延迟和实际战斗平衡仍待试玩确认。

本轮离线验收：assemble、stateTest、compileValidationJava 成功；221137 条新状态/区域断言与 36 条原状态断言通过；816 个姿态、1109760 次道具对身体箱体检查通过；98 次不同朝向/年龄的杖顶发射与下砸坐标对比通过；198 个 BB 关键帧与运行时数据一致。原书本悬浮、幽魂尾部、魂火/反向指针、贴图、BGM 与唱片检查通过。
